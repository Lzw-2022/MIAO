<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>致苗苗</title>
    <style>
        /* 全局样式 */
        body, html {
            margin: 0;
            padding: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            background: linear-gradient(120deg, #fccb90 0%, #d57eeb 100%); /* 浪漫的渐变背景 */
            font-family: 'Microsoft YaHei', 'PingFang SC', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* 容器 */
        .container {
            position: relative;
            text-align: center;
            z-index: 10; /* 保证在背景粒子之上 */
        }

        /* CSS画爱心 */
        .heart {
            position: relative;
            width: 100px;
            height: 90px;
            display: inline-block;
            /* 动画：生长持续4秒，之后无限循环跳动 */
            animation: grow 4s ease-out forwards, heartbeat 1.2s infinite 4s; 
            transform-origin: center center;
        }

        .heart::before,
        .heart::after {
            content: "";
            position: absolute;
            left: 50px;
            top: 0;
            width: 50px;
            height: 80px;
            background: #ff4b4b;
            border-radius: 50px 50px 0 0;
            transform: rotate(-45deg);
            transform-origin: 0 100%;
            box-shadow: 0 0 20px rgba(255, 0, 0, 0.4); /* 发光效果 */
        }

        .heart::after {
            left: 0;
            transform: rotate(45deg);
            transform-origin: 100% 100%;
        }

        /* 文字样式 */
        .message {
            margin-top: 50px;
            font-size: 2rem;
            font-weight: bold;
            color: #fff;
            text-shadow: 0 2px 10px rgba(0,0,0,0.2);
            opacity: 0; /* 初始不可见 */
            transform: translateY(20px);
            /* 动画：在爱心变大后的第3秒开始淡入 */
            animation: fadeInUp 2s ease-out forwards 3s;
            white-space: nowrap;
        }

        /* 爱心慢慢变大的动画 */
        @keyframes grow {
            0% {
                transform: scale(0);
            }
            100% {
                transform: scale(1.5); /* 最终大小 */
            }
        }

        /* 爱心跳动的动画 */
        @keyframes heartbeat {
            0% { transform: scale(1.5); }
            14% { transform: scale(1.65); }
            28% { transform: scale(1.5); }
            42% { transform: scale(1.65); }
            70% { transform: scale(1.5); }
        }

        /* 文字淡入上浮动画 */
        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* 背景漂浮的小粒子 */
        .bg-bubbles {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 1;
            pointer-events: none;
        }

        .bubble {
            position: absolute;
            bottom: -20px;
            background: rgba(255, 255, 255, 0.4);
            border-radius: 50%;
            animation: floatUp linear infinite;
        }
        
        /* 漂浮动画 */
        @keyframes floatUp {
            0% { transform: translateY(0); opacity: 0; }
            20% { opacity: 0.6; }
            80% { opacity: 0.6; }
            100% { transform: translateY(-100vh); opacity: 0; }
        }
    </style>
</head>
<body>

    <!-- 背景漂浮装饰 -->
    <div class="bg-bubbles" id="bg-bubbles"></div>

    <div class="container">
        <!-- 爱心 -->
        <div class="heart"></div>
        <!-- 文字 -->
        <div class="message">苗苗 我喜欢你</div>
    </div>

    <script>
        // 简单的脚本来生成背景漂浮物，增加氛围感
        function createBubbles() {
            const container = document.getElementById('bg-bubbles');
            const bubbleCount = 20; // 气泡数量

            for (let i = 0; i < bubbleCount; i++) {
                const bubble = document.createElement('div');
                bubble.classList.add('bubble');
                
                // 随机大小
                const size = Math.random() * 15 + 5 + 'px';
                bubble.style.width = size;
                bubble.style.height = size;
                
                // 随机位置
                bubble.style.left = Math.random() * 100 + '%';
                
                // 随机动画时间 (5s 到 15s)
                bubble.style.animationDuration = (Math.rando